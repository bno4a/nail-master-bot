services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: nailbot
      POSTGRES_USER: nailbot
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
